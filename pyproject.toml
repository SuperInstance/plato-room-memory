[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-memory"
version = "0.1.0"
description = "Per-room persistent memory with context windowing"
license = {text = "MIT"}
requires-python = ">=3.8"
